# Unified dependency set for the "video_agent" project.
# Install with:  uv pip install -r requirements.txt  (or pip install -r requirements.txt)

pyyaml>=6.0
httpx>=0.27
volcengine-python-sdk[ark]
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
boto3>=1.35
